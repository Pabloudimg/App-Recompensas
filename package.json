{
  "name": "app-recompensas",
  "private": true,
  "version": "0.3.7",
  "type": "module",
  "scripts": {
    "prepare:ui": "node scripts/apply-family-ui.cjs && node scripts/apply-day-ui.cjs && node scripts/apply-emoji-picker.cjs && node scripts/apply-default-birthdates.cjs",
    "dev": "npm run prepare:ui && vite --host 0.0.0.0",
    "build": "npm run prepare:ui && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^11.10.0",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "4.3.4",
    "vite": "5.4.11"
  }
}
